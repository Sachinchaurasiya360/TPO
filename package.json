{
  "name": "tpo_website",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "install:all": "npm install && cd backend && npm install && cd ../frontend/admin && npm install && cd ../student && npm install",
    "start": "cd backend && npm start",
    "dev": "cd backend && npm run dev",
    
    "build": "cd backend && npm run build && cd ../frontend/admin && npm run build && cd ../student && npm run build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "@types/node": "^24.8.1",
    "concurrently": "^8.2.2"
  },
  "dependencies": {
    "@tailwindcss/vite": "^4.1.14",
    "tailwindcss": "^4.1.14"
  }
}
